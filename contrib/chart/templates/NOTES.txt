Centaur K3s chart installed.

Important follow-ups:

1. This chart deploys the trusted control plane on K3s.
2. Agent sandboxes run as Kubernetes Pods managed by the API.
3. Infra Secrets must exist before install or upgrade; use `just bootstrap-secrets` for local dev.
4. `api.executionWorkerEnabled` and `api.victoriaMetricsPushEnabled` are disabled by default for the control-plane-first K3s shape.
5. Keep `runnerAccess.enabled=false` unless you have a private subnet and strict source ranges.

Useful commands:

  kubectl get pods -n {{ .Release.Namespace }}
  kubectl get ingress -n {{ .Release.Namespace }}
  helm template {{ .Release.Name }} {{ .Chart.Name }} -n {{ .Release.Namespace }} -f your-values.yaml
