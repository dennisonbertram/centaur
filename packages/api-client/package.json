{
  "name": "@centaur/api-client",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate:types": "UV_CACHE_DIR=${UV_CACHE_DIR:-/tmp/uv-cache} uv run --no-project --with pydantic python scripts/generate-agent-types.py",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@centaur/harness-events": "workspace:*",
    "axios": "^1.13.6",
    "eventsource-parser": "^3.0.6"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "^4.1.0"
  }
}
